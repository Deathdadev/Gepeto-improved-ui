# Example .env file for local setup
# Copy this file to .env and replace the placeholder values.

# --- Required ---

# Generate a strong secret using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# This is crucial for session security.
SESSION_SECRET=replace_with_a_strong_random_secret

# The *full* URL where your frontend application is running (e.g., http://localhost:8080).
# This MUST match the "Authorization callback URL" configured in your GitHub OAuth App settings.
FRONTEND_URL=http://localhost:8080

# Your GitHub OAuth App's client ID
# You can find this in your GitHub App settings
GITHUB_CLIENT_ID=your_github_client_id_here

# --- Optional ---

# The port the backend server will listen on (defaults to 3000 if not set)
# BACKEND_PORT=3000